spring:
  application:
    name: api-service

flowforge:
  security:
    jwt:
      secret: ${FLOWFORGE_JWT_SECRET:local_development_jwt_signing_key_at_least_32_bytes_long}
      expiration-ms: 3600000
  datasource:
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc.time_zone: UTC
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: when_authorized
