spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dummy_test_db
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
  kafka:
    bootstrap-servers: localhost:9092
    listener:
      auto-startup: false
